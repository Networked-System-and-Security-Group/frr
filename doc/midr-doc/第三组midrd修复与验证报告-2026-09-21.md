# 第三组 midrd 修复与验证报告

日期：2026-09-21
分支：`feat/yhy-midr-single-instance-ls-flooding`
HEAD（被验证的修复序列）：`7ad4671558`
本轮修复基线：`1adb4c92d0`
与 `origin/master` 的 merge-base：`4531f3490b`
审查依据：`doc/midr-doc/第三组最新midrd分支审查反馈与同步建议-2026-09-20.md`

## 0. 环境与同步链

> **后续轮次**：D1–D6 的处置、公共基线（第二组）补同步、D5 修复、门禁扩展与真实 zebra 交错注入，
> 见 `doc/midr-doc/第三组midrd第二轮修复与验证报告-2026-09-21.md`。
> 其中 D1 复核确认：本文 A.1 引用的 `/tmp/verify-build-7ad4671558.log` **当前在容器内存在**
> （`grep -c 'warning:'` = 13，`dplane_fpm_nl` 2 条；`T10` 日志 = 11 且 `grep -v bgpd/` 为空），
> "T9 前 13 条 → T9 后 11 条"可复核，无需补做基线从零构建。

| 项 | 值 |
| --- | --- |
| 源码工作树 | Mac `/Users/yangmengyu/githubdocuments/frr` |
| 构建/测试容器 | `frr-ubuntu24-ymy`（112 核，源码树 `/home/frr/frr-midrd3`） |
| GRE 对端容器 | `node1`、`node2`（镜像 `frr-ubuntu24-ymy:init`，均在 Up） |
| 同步链 | Mac rsync → 服务器 `~/frr-midrd3` → `docker cp` → 容器（`dp-02-build-and-test.md` 第 6 节） |
| 串行化 | 所有容器内同步/构建/门禁都在 `flock /tmp/midrd-team.lock` 下执行，避免与其他构建并发 |
| 容器内日志 | `/tmp/verify-<gate>-7ad4671558.log` |

本报告只记录**实际执行并观察到输出**的门禁；未执行项一律在 §4 明确列为 NOT RUN。

## 1. 审查条目 → 提交 → 文件 → 证据

| 审查条目 | 提交 | 文件 | 验证命令 | 观察到的摘要 | 日志 |
| --- | --- | --- | --- | --- | --- |
| 2.1 ZAPI 编号稳定 | `e7c78d33a7` | `lib/zclient.h`、`lib/log.c`、`zebra/zebra_dplane.c`、`zebra/zebra_rib.c`、`midr-test/check-zapi-numbering.sh` | `./midr-test/check-zapi-numbering.sh` | `check-zapi-numbering: PASS (baseline 1adb4c92d0 vs working tree)`，EXIT=0 | `/tmp/verify-zapi-numbering-7ad4671558.log` |
| 2.2 deferred FIB 失败后 desired/installed 一致 | `07023e068a` | `midrd/midr-dp-backend.c`、`midrd/midr-dp-backend.h`、`midrd/dp-backend-test.c` | `/tmp/midrd-build/midrd-dp-test`；`midrd/r7-dp-fib-smoke.sh` case B | `midrd-dp-test: PASS`；fib smoke `PASS=15 FAIL=0` / `third-group ZAPI/FIB smoke: PASS` | `/tmp/verify-dp-test-7ad4671558.log`、`/tmp/verify-fib-smoke-7ad4671558.log` |
| 2.3 恢复重试达上限后不再停滞 | `07023e068a`（T2/T3 合并在同一提交，见 §5） | 同上 | `/tmp/midrd-build/midrd-dp-test` | 用例 `test_recovery_past_cap_rearm`：`send-failures=51 … slow heartbeat armed`，随后 `midrd-dp-test: PASS` | `/tmp/verify-dp-test-7ad4671558.log` |
| 3.1 `dp_installed_set()` 分配失败处理 | `64177b16a6` | `midrd/midr-dp-backend.c`、`midrd/midr-dp-backend.h`、`midrd/dp-backend-test.c` | `/tmp/midrd-build/midrd-dp-test` | `midrd-dp-test: PASS`（含 `test_installed_set_failure`） | `/tmp/verify-dp-test-7ad4671558.log` |
| 3.2 backend ops 生命周期（不保存调用方指针） | `7ad4671558` | `midrd/midr-zebra.c`、`midrd/midr-zebra.h`、`midrd/midr-context-private.h` | 组件套件 + `midrd-dp-test` | `midrd-dp-test: PASS`，`midrd-dp-gre-registry-test: PASS` | `/tmp/verify-midrd-suite-7ad4671558.log`、`/tmp/verify-dp-test-7ad4671558.log` |
| 3.3 `midr_gre_registry_add()` `calloc()` 失败 | `d5838d4edf` | `midrd/midr-gre.c`、`midrd/midr-gre.h`、`midrd/gre-registry-test.c`、`midrd/Makefile` | 组件套件 | `midrd-dp-gre-registry-test: PASS` | `/tmp/verify-midrd-suite-7ad4671558.log` |
| 3.4 同名接口绑定新端点时更新 registry | `d5838d4edf` | `midrd/midr-gre.c` | 组件套件 + `midrd/midr-gre-connectivity-test.sh` | `midrd-dp-gre-registry-test: PASS`；GRE connectivity `PASS=24 FAIL=0` | `/tmp/verify-midrd-suite-7ad4671558.log`、`/tmp/verify-gre-connectivity-7ad4671558.log` |
| 3.5 文档 trailing whitespace / 末尾空行 | 本轮 boundary 5（未提交，见 §3） | `doc/midr-doc/**` 中本分支新增的 55 个文件 | `git diff --check`；perl trailing-ws 扫描 | `git_diff_check_exit=0`，`perl_scan_lines=0` | `/tmp/verify-diff-check-7ad4671558.log` |

§5「修复后的最小验收集」逐条对应 §2 的 A.1–A.9 门禁，执行结果见下。

## 2. 门禁执行明细

### A.1 FRR 顶层构建（从零重建）

```
cd /home/frr/frr-midrd3 && make clean && make -j112      # 容器 frr-ubuntu24-ymy，root，flock 内
```

- `clean_exit=0`（`/tmp/verify-clean-7ad4671558.log`）
- `build_exit=0`；日志 1205 行；`CC` 行 925、`CCLD` 48；`lib/.libs/libfrr.so.0.0.0`、`zebra/.libs/zebra`、`bgpd/.libs/bgpd` 时间戳均为本次 06:14 → **确认为完整 clean 重建**，`lib/zclient.h` 的所有依赖对象都重新编译过。
- **告警数（T9 前）：13 行含 `warning:`，不是 0。** 分两类（T9 后的最终数字见下方复测）：

  (a) 与本分支 diff 相关、仍未修完的 DPLANE 开关告警 2 条：

  ```
  zebra/dplane_fpm_nl.c:978:9: warning: enumeration value 'DPLANE_OP_GRE_ADD' not handled in switch [-Wswitch]
  zebra/dplane_fpm_nl.c:978:9: warning: enumeration value 'DPLANE_OP_GRE_DELETE' not handled in switch [-Wswitch]
  ```

  (b) 与本分支 diff 无关、预先存在的 bgpd midr 告警 11 条：`bgpd/bgp_midr_pm.c` ×2（`-Wbad-function-cast`）、`bgpd/midr_trace_scheduler.c:1682` ×8（`-Wswitch-enum`）、`bgpd/bgp_midr_nds.c:3375` ×1（`-Wunused-function`）。这些文件都不在本分支的改动集内。

- **基线 `1adb4c92d0` 的行为**：基线 `lib/zclient.h` 第 233–234 行就已经把 `ZEBRA_GRE_ADD`/`ZEBRA_GRE_DELETE` 插在 enum 中间（正是审查 2.1 的缺陷），而 `zebra/dplane_fpm_nl.c` 在基线与修复树中都只有 `DPLANE_OP_GRE_SET` 一个 GRE case。`e7c78d33a7` 只补了 5 个开关（`zebra/zebra_dplane.c` 4 个 + `zebra/zebra_rib.c` 1 个）。因此：基线从零构建**并非 warning-free**（至少含这 5 个开关的 GRE 告警 + fpm_nl 的 2 条）；修复树把同类告警从 ≥7 条降到仍剩 2 条，**并未达到 0**。本环境未对基线做一次完整的从零构建（见 §4 NOT RUN），所以基线数字来自源码对照而非基线构建日志。
- 日志：`/tmp/verify-build-7ad4671558.log`、`/tmp/verify-clean-7ad4671558.log`

**T9 后复测（最终实测）**：zebra owner 在 T9 把 `DPLANE_OP_GRE_ADD`/`DPLANE_OP_GRE_DELETE`
补进 `zebra/dplane_fpm_nl.c` 的 no-op 分组（第 1098-1100 行，`case DPLANE_OP_GRE_SET:`
旁）；本执行者同步后在同一容器、`flock /tmp/midrd-team.lock` 内再次执行
`make clean && make -j112`：`clean_exit=0`、`build_exit=0`、日志 **1199 行**、
`grep -c 'warning:'` = **11**，且 `grep warning: … | grep -v bgpd/` 为空、
`grep warning: … | grep dplane` **0 命中**。即本分支自身 DPLANE 相关告警 **2→0**，
剩余 **11 条全部为分支外既有 bgpd 告警**（OUT OF SCOPE，见 §6）。日志
`/tmp/verify-clean-T10.log`、`/tmp/verify-build-T10.log`。

### A.2 midrd 组件套件

```
cd /home/frr/frr-midrd3/midrd && make -j112 BUILD_DIR=/tmp/midrd-build test
```

- 摘要：**25 个组件程序 PASS + `standalone libfrr boundary scan: PASS`（共 26 行 `: PASS`），`FAIL` 出现 0 次，`EXIT=0`**。套件确已因修复增长：新增 `midrd-dp-gre-registry-test: PASS`。
- 程序：`midrd R6-A contract`、`midr-core-test`、`midr-prefix-provider-test`、`midrd-wire-test`、`midrd-transport-test`、`midrd-transport-budget-test`、`midrd session tests`、`midrd-consumer-test`、`midrd-spf-test`、`midrd-engine-test`、`midrd-prefix-ipc-test`、`midrd-owned-test`、`midrd-prefix-transaction-test`、`midrd-scope-test`、`midrd-cost-test`、`midrd-local-ipc-test`、`midrd-local-transaction-test`、`midrd-lsdb-test`、`midrd-ted-test`、`midrd-interface-test`、`midrd-spf-install-test`、`midrd-dp-test`、`midrd-dp-gre-registry-test`、`midrd-sync-test`、`midrd-scale-test`。
- 日志：`/tmp/verify-midrd-suite-7ad4671558.log`（末行 `EXIT=0`）

### A.3 ZAPI 编号回归（Mac 侧）

```
cd /Users/yangmengyu/githubdocuments/frr && ./midr-test/check-zapi-numbering.sh
```

- 输出：
  ```
  note: 147 reference enum members; 141 tolerated pre-existing positional gap(s) (unchanged vs baseline, e.g. ZEBRA_INTERFACE_SET_ARP has no DESC_ENTRY)
  check-zapi-numbering: PASS (baseline 1adb4c92d0 vs working tree)
  ```
  EXIT=0。
- 该门禁逐条比对：147 个既有枚举成员序号不变；新增成员只能且正是尾部追加的 `ZEBRA_GRE_ADD`/`ZEBRA_GRE_DELETE`；`lib/log.c` 的 `command_types[]` 源码顺序仍随枚举序号单调；位置错位集合与基线一致；非 GRE 的 `DESC_ENTRY` 未重排。
- 日志：`/tmp/verify-zapi-numbering-7ad4671558.log`（Mac 侧 `/tmp/verify-logs/verify-zapi-numbering-7ad4671558.log`）

### A.4 原生三节点 IPv4 / IPv6 / dual-stack smoke

```
cd /home/frr/frr-midrd3/midrd && MIDRD_BIN=/tmp/midrd-build/midrd bash ./r7-native-smoke.sh
```

- 摘要：
  ```
  r7 native smoke ipv4: PASS (logs: /tmp/midrd-native-ipv4.ACykPB)
  r7 native smoke ipv6: PASS (logs: /tmp/midrd-native-ipv6.fvjo6z)
  r7 native dual-stack smoke: PASS
  native_exit=0
  ```
- 日志：`/tmp/verify-native-7ad4671558.log`

### A.5 GRE 连通性（docker 宿主，root）

```
sudo bash /home/yangmy/frr-midrd3/midrd/midr-gre-connectivity-test.sh
```

- 摘要：`=== summary: PASS=24 FAIL=0 ===`，`GRE_EXIT=0`。覆盖 IPv4 GRE 承载 v4/v6 overlay、ip6gre、establishment 状态/ifindex 幂等、teardown 删除。
- **脚本改动（`midrd/midr-gre-connectivity-test.sh`，属本次允许修改范围）**：`BUILD_TOOL` 原先硬编码为 `/tmp/midrd-build-harness/midrd-gre-tool`，该路径确实存在但是 2026-09-19 的**陈旧**二进制，会让门禁测的是修复前的 `midr-gre.c`。现改为可被 `MIDRD_GRE_TOOL` 覆盖，并在容器内按 `/tmp/midrd-build/midrd-gre-tool` → `/tmp/midrd-build-harness/midrd-gre-tool` 顺序探测，找不到时显式报错退出。本次运行日志确认使用了新构建产物：`using gre-link-tool: /tmp/midrd-build/midrd-gre-tool`。
- 日志：`/tmp/verify-gre-connectivity-7ad4671558.log`

### A.6 containerlab 三节点集成

```
sudo bash /home/yangmy/frr-midrd3/midrd/r7-dp-integration-smoke.sh     # 必须 root
```

- 摘要：`=== summary: PASS=31 FAIL=0 (logs: /tmp/midrd-dp-integration/run) ===`，`INTEGRATION_EXIT=0`。
- 修正过程：首次以普通用户运行，脚本在清理上一次 root 运行留下的 `/tmp/midrd-dp-integration/run`（root 属主）时报 `rm: … 权限不够` 并以 EXIT=1 中止（PASS=0，未执行任何用例），这不是产品失败；按脚本文档要求（docker 宿主 root）以 root 重跑后 31/31 通过。
- 日志：`/tmp/verify-integration-7ad4671558.log`

### A.7 真实 zebra / 内核 FIB smoke（容器内 root）

```
cd /home/frr/frr-midrd3/midrd && bash ./r7-dp-fib-smoke.sh
```

- 摘要：`=== summary: PASS=15 FAIL=0 ===`，末行 `third-group ZAPI/FIB smoke: PASS`，`fib_exit=0`。工具行 `tool=/tmp/midrd-build/midrd-dp-e2e-tool zebra=/home/frr/frr-midrd3/zebra/.libs/zebra`。
- **路径结论**：无需再改路径。`zebra/.libs/zebra` 现已存在；脚本通过 `FRR_ROOT`/`ZEBRA_BIN`/`DP_TOOL_BIN` 环境变量覆盖，默认值分别是 `$ROOT/zebra/.libs/zebra` 与 `/tmp/midrd-build/midrd-dp-e2e-tool`，两者本次均存在且为 06:14 新构建。审查中「脚本指定的 zebra 路径不存在」的跳过理由已过时。
- 日志：`/tmp/verify-fib-smoke-7ad4671558.log`

### A.8 失败恢复 / 交错场景

**(a) `midrd-dp-test` 直接运行（需要活动 event master）**

```
cd /home/frr/frr-midrd3/midrd && /tmp/midrd-build/midrd-dp-test
```

用例由 `main()` 顺序调用（`midrd/dp-backend-test.c:993-1005`）：`test_encoding_modes`、`test_send_failure_and_abort`、`test_adapter_pipeline_and_recovery`、`test_deferred_partial_batch_failure`、`test_deferred_whole_batch_failure`、`test_deferred_failure_across_reconnect`、`test_recovery_with_new_desired_inflight`、`test_recovery_past_cap_rearm`、`test_installed_set_failure`、`test_gre_api`。全部由 `assert()` 保护，任一失败会直接 abort；本次到达末行 **`midrd-dp-test: PASS`**，`dp_test_exit=0`。

逐字引用的 DP 计数行（每个场景一行）：

```
dp backend=stop zebra=down installed=1 pending=0 route-adds=7 route-deletes=6 send-failures=1 replays=0 resyncs=0 recovery=idle last-error=-113
dp backend=stop zebra=down installed=0 pending=0 route-adds=1 route-deletes=1 send-failures=3 replays=0 resyncs=0 recovery=idle last-error=-107
MIDR data plane: batch submission failed (-5), retained 2 ops for recovery
dp backend=stop zebra=up installed=0 pending=0 route-adds=3 route-deletes=2 send-failures=1 replays=1 resyncs=1 recovery=idle last-error=0
dp backend=stop zebra=up installed=0 pending=0 route-adds=2 route-deletes=1 send-failures=2 replays=1 resyncs=1 recovery=idle last-error=0
MIDR data plane: retained batch still failing after 50 retries (-5), slow heartbeat armed
dp backend=stop zebra=down installed=0 pending=0 route-adds=3 route-deletes=3 send-failures=51 replays=0 resyncs=1 recovery=idle last-error=0
dp backend=stop zebra=down installed=1 pending=0 route-adds=1 route-deletes=0 send-failures=1 replays=0 resyncs=0 recovery=idle last-error=-12
```

每个场景**同时比较**了什么（显式说明，避免“只看返回值”）：

| 场景 | 比较对象 |
| --- | --- |
| 部分批次失败 `test_deferred_partial_batch_failure` | 保留的 pending 数 + installed 集合 + 断言 `resyncs==1`（`dp-backend-test.c:666`）：失败批次被保留，并由一次显式 resync 完整重放 |
| 整批失败 `test_deferred_whole_batch_failure` | 保留余量 + `replays==1`（仅保留批次重发，`:703`）：收敛到与 desired 相同的 installed 集合 |
| 重连恢复 `test_deferred_failure_across_reconnect` | `dp.replays==1` 且 `dp.resyncs==1`（`:755/:760`）+ 重连后 installed 集合与 desired 一致 |
| 完整 replay/resync `test_adapter_pipeline_and_recovery` | 同代 resync 为 no-op（`:493`）；重连后 `replays==1`（`:500`）；新代在 deferred 定时器内首次失败时只有保留批次重试能收敛 FIB，断言 `resyncs==1`（`:551`） |
| 恢复期间新 desired `test_recovery_with_new_desired_inflight` | 同时比较 `spf.desired_generation`（gen8→gen9 必须变化，`:799/:809`）、`dp.pending` 2→4、`dp.installed`、实际下发的 ZAPI 命令序列 `captures[1..4] = DELETE,ADD,DELETE,ADD`（`:819-822`，FIFO 收敛）以及最终 `!dp.recovering && last_error==0` |
| 达上限不再停滞 `test_recovery_past_cap_rearm` | `recovery_stalled` + `recovery_attempts>=50` + `installed==1 && pending==2` + `last_error==-EIO`（`:853-856`），随后由新 desired 再次触发同步并收敛 |
| installed 集合分配失败 `test_installed_set_failure` | 分配失败被上报（错误码）且状态机不残留半安装状态 |

**(b) 真实 zebra + 内核 FIB 的重连 / 撤销证据（FIB smoke case B）**

```
==== B. TED -> SPF -> adapter -> FIB, zebra restart replay, shutdown withdraw ====
[PASS] synthetic committed TED applied (SPF -> adapter -> backend)
[PASS] FIB: 198.51.100.0/24 via 192.0.200.2 (SPF-derived, proto 199)
[PASS] zebra restarted
[PASS] reconnect replay restored the route after the zebra restart
action=serve-ted-tick
[PASS] SIGTERM withdrew the SPF route (unregister + flush)
dp backend=stop zebra=up installed=0 pending=0 route-adds=2 route-deletes=1 send-failures=0 replays=2 resyncs=0 recovery=idle last-error=0
DP action=shutdown prefix=- rc=0 installed=0 adds=0 dels=0 fails=0
```

三面一起核对：

- 控制面 desired：serve-ted 的 SPF 结果给出期望路由 `198.51.100.0/24 via 192.0.200.2`；
- 已安装 FIB 集合：用 `ip route` 实际读取 proto 199 的内核条目（脚本 `[PASS] FIB: …`），而不是只看工具返回码；
- 最终一致性：SIGTERM 撤销后 `installed=0 pending=0`，且 FIB 中该路由消失（`[PASS] SIGTERM withdrew…`）；重启后 `replays=2` 且同一条目重新出现。

诚实边界：FIB smoke 的 DP 计数行**不含 desired generation 字段**（格式为 `backend zebra installed pending route-adds route-deletes send-failures replays resyncs recovery last-error`），显式的 desired-generation 断言在 dp-test 的 `midr_spf_install_status_get().desired_generation` 上；且 FIB smoke 本次只覆盖“重连 replay + 撤销”，未在真实 zebra 上单独注入 deferred partial-batch 失败——那 4 个交错用例由上面 (a) 的 dp-test 覆盖。

日志：`/tmp/verify-dp-test-7ad4671558.log`、`/tmp/verify-fib-smoke-7ad4671558.log`

### A.9 边界扫描 / 构建产物扫描 / whitespace

- `cd /home/frr/frr-midrd3/midrd && bash ./extraction-boundary-test.sh` → `standalone libfrr boundary scan: PASS`，`boundary_exit=0`；日志 `/tmp/verify-boundary-7ad4671558.log`（首次日志被容器内其它脚本清理，已重跑并重新生成）。
- `./midr-test/check-source-build-artifacts.sh`（Mac）→ `SOURCE_ARTIFACT_SCAN=clean root=/Users/yangmengyu/githubdocuments/frr`，`EXIT=0`。附注：macOS BSD `find` 不支持 `-perm /111`，脚本最后一段 `tests/bgpd` 子扫描在 Mac 上会打印 `find: -perm: /111: illegal mode string` 后被跳过（脚本仍 exit 0）；以 macOS 兼容写法重跑该子扫描：`tests/bgpd` 下 `test_midr_*` 可执行文件 0 个，worktree 内 `*.o/*.lo/*.la/.deps/.libs/.dirstamp` 0 个。
- `git diff --check` → `git_diff_check_exit=0`；perl trailing-whitespace 扫描 0 行；`git diff --cached --check` → 0（已 stage 的审查记录文档本身无空白问题）。
- 日志：`/tmp/verify-diff-check-7ad4671558.log`、`/tmp/verify-source-artifacts-7ad4671558.log`

## 3. 文档清理（审查 3.5）

**范围**（本分支新增的文档集）：`git diff --name-status $(git merge-base HEAD origin/master) HEAD -- doc/midr-doc/` 全部 55 个 `A` 条目，其中 `dp-doc/` 下的文件天然包含在新增集合里；未新增的共享文档未被触碰。`doc/midr-doc/第三组最新midrd分支审查反馈与同步建议-2026-09-20.md` 是本次新加入（`git add`，未提交）的文件，扫描显示它本身 0 处 trailing whitespace、恰好 1 个结尾换行，因此**未修改其内容**。

只做了两件事：去掉行尾空格/Tab、把连续结尾空行收敛为单个结尾换行；中文正文与其余内容按字节保持一致（`git diff --ignore-all-space --ignore-blank-lines --stat` 为空，即不存在非空白改动）。

合计：**49 处 trailing-whitespace 行（13 个文件）**、**12 个文件共 13 个多余结尾空行**，落在 22 个文件（同一内容在 `doc/midr-doc/` 与 `doc/midr-doc/dp-doc/` 各有一份，故计数翻倍）；`git diff --shortstat` = `22 files changed, 49 insertions(+), 62 deletions(-)`。

| 文件 | 行数 before→after | 去尾空格行 | 去多余结尾空行 |
| --- | --- | --- | --- |
| doc/midr-doc/dp-doc/README-dp-migration.md | 354→353 | 1 | 1 |
| doc/midr-doc/dp-doc/dp-01-migration-overview.md | 157→156 | 0 | 1 |
| doc/midr-doc/dp-doc/dp-02-build-and-test.md | 206→205 | 0 | 1 |
| doc/midr-doc/dp-doc/dp-03-e2e-and-integration.md | 281→280 | 0 | 1 |
| doc/midr-doc/dp-doc/dp-04-gre.md | 198→197 | 0 | 1 |
| doc/midr-doc/dp-doc/midr-cp-dp-interface.md | 215→214 | 0 | 1 |
| doc/midr-doc/dp-doc/midr-dataplane-design-and-implementation.md | 419→419 | 2 | 0 |
| doc/midr-doc/dp-doc/midr-dataplane-dev-doc.md | 526→525 | 2 | 1 |
| doc/midr-doc/dp-doc/midr-dataplane-progress-report.md | 766→766 | 3 | 0 |
| doc/midr-doc/dp-doc/midr-gre-interface-api.md | 310→308 | 0 | 2 |
| doc/midr-doc/dp-doc/midr-gre-interface.md | 465→464 | 0 | 1 |
| doc/midr-doc/dp-doc/midr-gre-test-report.md | 215→214 | 0 | 1 |
| doc/midr-doc/dp-doc/midr-test-report-20260709.md | 355→355 | 13 | 0 |
| doc/midr-doc/dp-doc/test-steps/02-netlink-c-proto199.md | 112→112 | 2 | 0 |
| doc/midr-doc/dp-doc/traffic-engineering.md | 715→715 | 2 | 0 |
| doc/midr-doc/midr-cp-dp-interface.md | 215→214 | 0 | 1 |
| doc/midr-doc/midr-dataplane-design-and-implementation.md | 419→419 | 2 | 0 |
| doc/midr-doc/midr-dataplane-dev-doc.md | 526→525 | 2 | 1 |
| doc/midr-doc/midr-dataplane-progress-report.md | 766→766 | 3 | 0 |
| doc/midr-doc/midr-test-report-20260709.md | 355→355 | 13 | 0 |
| doc/midr-doc/test-steps/02-netlink-c-proto199.md | 112→112 | 2 | 0 |
| doc/midr-doc/traffic-engineering.md | 715→715 | 2 | 0 |
| **合计** | — | **49** | **13** |

清理后：`git diff --check` exit 0；perl `[ \t]+$` 扫描 0 行；13 个「49 行 / 13 文件」的计数与审查 3.5 的描述逐字吻合。

> 备注：Markdown 中行尾两个空格是硬换行语义，本次按审查要求一律去除；这些文件的正文是说明性文字，未依赖硬换行排版。

## 4. 本次环境已执行 / NOT RUN

已执行（每条都有上面的命令、摘要与日志）：

| 门禁 | 结果 |
| --- | --- |
| A.1 FRR 顶层从零构建 | 已执行，EXIT=0，**11 条告警（全部 bgpd、非 0）**；T9 前 13（含 2 条 DPLANE，已修） |
| A.2 midrd 组件套件 | 已执行，25 程序 + 边界扫描 PASS，EXIT=0 |
| A.3 ZAPI 编号回归 | 已执行，PASS |
| A.4 原生三节点 v4/v6/dual-stack | 已执行，3/3 PASS |
| A.5 GRE 连通性 | 已执行，PASS=24 FAIL=0 |
| A.6 containerlab 集成 | 已执行（root），PASS=31 FAIL=0 |
| A.7 真实 zebra/FIB smoke | 已执行，PASS=15 FAIL=0 |
| A.8 失败恢复交错 | 已执行，dp-test PASS + FIB smoke case B PASS |
| A.9 边界/产物/whitespace | 已执行，全部 PASS/clean |

**NOT RUN（不当作通过）**：

- 基线 `1adb4c92d0` 的一次完整从零构建：未执行；基线告警结论由 `lib/zclient.h` / `zebra/dplane_fpm_nl.c` 的源码对照得出（§2 A.1）。
- `midr-test/check-zapi-numbering.sh` 的 `ZAPI_NUMBERING_VERBOSE=1` 全量 dump：未执行（门禁已 PASS，仅诊断用途）。
- `check-source-build-artifacts.sh` 中 `tests/bgpd` 子扫描在 **macOS** 上的原样执行：BSD `find` 不支持 `-perm /111`，该子扫描被跳过；已用等价写法手工执行（0 命中）。脚本本身未改（不属“路径需修正”）。
- 真实 zebra 上的 deferred partial/whole-batch 失败注入：未单独执行；该语义由 dp-test 的 4 个交错用例覆盖（§2 A.8）。
- 未执行的门禁不得记为通过；以上均明确标注。

## 5. 偏差记录：T2 / T3 合并在同一提交

审查 §4 建议把「deferred failure 后的 desired/install 一致性」（2.2）与「recovery 重试上限后再次触发」（2.3）拆成两个提交，实际由 **`07023e068a` 一个提交**完成。原因：两者是同一条恢复状态机（保留批次 + 重试计数 + 停滞/重臂 + 新代追加）的不同切面，拆开会产生一个内部不自洽的中间态。因此这也是对审查提交边界的**有意偏差**，在此显式记录；对应的用例分别是 `test_deferred_*`（2.2）与 `test_recovery_past_cap_rearm`（2.3），两者都在 `midrd/dp-backend-test.c` 内并被本次门禁覆盖。

## 6. 遗留问题 / BLOCKERS

1. **DPLANE 开关告警已由 T9 修复（已解决）**：`zebra/dplane_fpm_nl.c` 的 switch 原缺 `DPLANE_OP_GRE_ADD`/`DPLANE_OP_GRE_DELETE`（与 `e7c78d33a7` 已修的 5 个开关同类）；T9 已在第 1098-1100 行的 no-op 分组补齐。T9 前从零重建 13 条告警，T9 后复测 **11 条（全部 bgpd）**、`grep dplane` 0 命中，**本分支自身 DPLANE 相关告警归零**。证据 `/tmp/verify-build-T10.log`。
2. 另外 11 条告警来自本分支 diff 之外的既有 bgpd midr 代码（`bgpd/bgp_midr_pm.c` ×2 `-Wbad-function-cast`、`bgpd/midr_trace_scheduler.c:1682` ×8 `-Wswitch-enum`、`bgpd/bgp_midr_nds.c:3375` ×1 `-Wunused-function`），非本分支引入，未处理。按审查 §6「不在本次同步范围内的内容」（禁止把第一组 group1 接线 / peer discovery / session observer 拉进第三组），这些**属 pre-existing、明确 OUT OF SCOPE**，不计入本分支修复验收。
3. 3 个（内容重复后为 6 个）文档文件本来就没有结尾换行（`\ No newline at end of file`）；审查缺陷只覆盖「trailing whitespace + 多余结尾空行」，故保持原样未改。
4. 容器内 `/tmp/verify-boundary-7ad4671558.log` 首次生成后被容器内其它脚本清理，已重跑并重新生成（结果一致）。
5. 本报告本身已 `git add`（staged，未提交）以便 worktree 无 untracked 文件；22 个文档空白清理文件与 `midrd/midr-gre-connectivity-test.sh` 目前**未 staged**，协调者提交 boundary 5 时需一并纳入（例如 `git add -A doc/midr-doc midrd/midr-gre-connectivity-test.sh`），否则会漏掉清理改动。**T10 的告警结论更正**（`dp-doc/dp-02-build-and-test.md`、`dp-doc/README-dp-migration.md`、两份 `*/test-steps/01-bgpd-build-verification.md`、`midr-second-group-close.md`）也在同一批 `doc/midr-doc/**` 改动内，一并纳入即可。
