! Node g1c: Group 1 member
! Transport 10.10.13.2 | ASN 65013 | Router-ID 10.0.13.1
frr version 10.0
hostname node-g1c
log file logs/bgpd-g1c.log debugging
log timestamp precision 3

debug bgp midr

router bgp 65013
  bgp router-id 10.0.13.1
  no bgp ebgp-requires-policy
  no bgp network import-check

  neighbor 10.10.11.2 remote-as 65011
  neighbor 10.10.11.2 ebgp-multihop 3

  address-family link-state link-state
    distribute bgp-fabric-link-state
    neighbor 10.10.11.2 activate
  exit-address-family

  midr transport-address 10.10.13.2
  midr group-id 1
  ! 删探测 A 后（发现链专题 2026-08-21）：认识群友的唯一途径是 join 流程向引导
  ! 要成员表，故凡参与 MIDR 的节点都必须配引导——原先靠"见到同群 NLRI 就建连"
  ! 兜底，那条路已删。g1a 是本台子的引导。
  midr bootstrap 10.10.11.2 remote-as 65011 router-id 10.0.11.1
!
