! Node e: singleton group-13 rep. Higher delay than d, so REP_PROBE_DONE
! ranks it as f's sole runner-up (ANCHOR) candidate rather than the primary.
! Will be killed mid-test to help isolate f.
! Transport 10.30.3.2 | ASN 65203 | Router-ID 10.0.203.1
frr version 10.0
hostname node-iso-e
log file logs/bgpd-e.log debugging
log timestamp precision 3

debug bgp midr

router bgp 65203
  bgp router-id 10.0.203.1
  no bgp ebgp-requires-policy
  no bgp network import-check

  address-family link-state link-state
  exit-address-family

  midr transport-address 10.30.3.2
  midr group-id 13
  midr role group-rep
  midr rep group 13 transport 10.30.3.2 remote-as 65203
!
