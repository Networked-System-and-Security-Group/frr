! Generated by gen-midrd-lab.py; do not edit by hand.
frr version 10.7.0-dev
frr defaults traditional
hostname r2
log file /etc/frr/logs/frr.log
!
ipv6 forwarding
no ip forwarding
!
! eth1 -> t2
interface eth1
 ipv6 address fd00:10:24::1/64
!
interface lo
 ipv6 address fd00:99::121/128
 ipv6 address fd00:18::121/128
!
! RFC 8212：本台子**不**用 `no bgp ebgp-requires-policy`（那开关是整实例的，
! 写了连动态 overlay 会话一起放行，8212 就永不触发）。underlay 邻居按真实网络
! 的做法显式挂 permit 策略；overlay 自动会话有意不预放行——它正是 8212 要考的。
ipv6 prefix-list UNDERLAY-LOOPBACKS seq 5 permit fd00:99::/64 ge 128 le 128
ipv6 prefix-list MIDR-SERVICE seq 5 permit fd00:18::121/128
route-map UNDERLAY-PERMIT permit 10
 match ipv6 address prefix-list UNDERLAY-LOOPBACKS
route-map MIDR-PREFIX permit 10
 match ipv6 address prefix-list MIDR-SERVICE
!
router bgp 65121
 bgp router-id 10.0.0.121
 no bgp default ipv4-unicast
 neighbor fd00:10:24::2 remote-as 65202
 !
 address-family ipv6 unicast
  neighbor fd00:10:24::2 activate
  redistribute connected
  neighbor fd00:10:24::2 route-map UNDERLAY-PERMIT in
  neighbor fd00:10:24::2 route-map UNDERLAY-PERMIT out
 exit-address-family
 !
 ! 〔件②（轮 4）删去 address-family link-state link-state 块（原含 distribute
 !   bgp-fabric-link-state）：MIDR 拓扑改由第二组的 (4,9) midr-ls 承载，其激活
 !   在代码里做（bgp_midr_ctrl.c 的整形 helper），不需要 conf 配置。不是漏配。〕
 !
!
! MIDR（midrd 第一组）：原先写在 router bgp 下的配置
debug midr discovery
midr transport-address fd00:99::121
midr group-id 2
midr role group-rep
midr session fd00:99::111 remote-as 65111
midr session fd00:99::192 remote-as 65192
midr bootstrap fd00:99::101 remote-as 65101 router-id 10.0.0.101
midr bootstrap fd00:99::102 remote-as 65102 router-id 10.0.0.102
midr bootstrap fd00:99::103 remote-as 65103 router-id 10.0.0.103
midr bootstrap fd00:99::104 remote-as 65104 router-id 10.0.0.104
midr bootstrap fd00:99::105 remote-as 65105 router-id 10.0.0.105
!
