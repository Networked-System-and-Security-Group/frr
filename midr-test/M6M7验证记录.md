# M6+M7 验证记录

基线：`4c625abe34`

阶段范围：Prefix Contributor、Node Prefix、代表节点与 Group Prefix、Production TED parity、Consumer Harness 和配置命令参考。不包含 SPF、计算调度、ZAPI/FIB 或路由安装。

## 强制验证

```bash
make -j$(nproc) \
  bgpd/bgpd \
  vtysh/vtysh \
  tests/bgpd/test_midr_prefix \
  tests/bgpd/test_midr_owned \
  tests/bgpd/test_midr_lsdb

./tests/bgpd/test_midr_prefix
./tests/bgpd/test_midr_owned
./tests/bgpd/test_midr_lsdb
./midr-test/run.sh prefix-config
python3 ./midr-test/run-m6-m7-scenarios.py
python3 ./midr-test/check-command-reference.py
```

已验证的关键行为：

- IPv4/IPv6、本地 AS_PATH 长度 0、一跳 BGP route、default route 和更长路径的显式放宽。
- stale、invalid、selected/multipath、AS_PATH 默认上限与按 AF 配置放宽。
- route-map 未配置时不增加过滤，配置后的 missing、deny 和 permit 行为。
- `ZEBRA_ROUTE_MIDR` 与其它 route type 使用相同 Contributor 准入规则。
- Prefix scan、`0->N/N->N/N->0`、Node Prefix sequence 和 reconcile domain。
- 多 Contributor、最后 Contributor 撤销、最小 Router-ID 代表和 takeover delay。
- 非代表 Group Prefix usable gate、Prefix OUT_OF_SYNC 撤销和恢复。
- 三节点真实 Session 上的 Prefix 传播、逐跳撤销和代表失联接管。
- Real/Mock 六类 TED 数组逐字段一致。
- Consumer 注册、首次主动获取、callback、引用释放、generation 当前性和注销。

## 阶段状态

```text
M6: COMPLETE
M7 engineering and automated QA: COMPLETE
M7 path-computation joint verification: PENDING
```

最终 commit、Clang、ASAN/UBSAN、全量 M0-M5 回归和审查结论在阶段关闭时补录。
