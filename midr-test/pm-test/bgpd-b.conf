! Node B — MIDR PM test
frr version 10.0
hostname node-b
log file bgpd-b.log debugging
log timestamp precision 3

debug bgp midr
debug bgp link-state

router bgp 65002
  bgp router-id 10.0.0.2
  no bgp ebgp-requires-policy
  no bgp network import-check

  ! BGP-LS peering with Node A (Rattan veth addresses)
  neighbor 10.1.1.1 remote-as 65001
  neighbor 10.1.1.1 ebgp-multihop 5
  neighbor 10.1.1.1 update-source 10.2.1.1

  address-family link-state link-state
    distribute bgp-fabric-link-state
    neighbor 10.1.1.1 activate
  exit-address-family

  ! Keep transport separate from the physical static-neighbor address.
  midr transport-address 10.0.0.2
  midr group-id 1
  ! start-node-b.sh creates the overlay after callback registration.
!
